name: CD - Deploy to Develop

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Confirmar despliegue en Develop"
        required: true
        default: "yes"
        type: choice
        options:
          - "yes"
          - "no"

jobs:
  deploy-develop:
    name: Deploy to Develop
    runs-on: ubuntu-latest
    environment: develop
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: develop

      - name: Deploy
        run: |
          echo "🚀 Este tiene que ser aprobado por los Admins para desplegarse"
          echo "📦 Desplegando en entorno: DEVELOP"
          echo "👤 Ejecutado por: ${{ github.actor }}"
          echo "🔀 Rama: develop"
